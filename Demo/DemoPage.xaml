<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:local="clr-namespace:Demo" x:Class="Demo.DemoPage">
	<Label Text="Welcome to Xamarin Forms!" VerticalOptions="Center" HorizontalOptions="Center" />
    <StackLayout Padding="20,20,20,20" VerticalOptions="FillAndExpand">
        <ListView x:Name="lvCards">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding name}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Button x:Name="bGetCards" Text="Get Cards" Clicked="BGetCards_OnClicked"></Button>
    </StackLayout>
</ContentPage>
